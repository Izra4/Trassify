package com.praktikum.trassify.ui.theme

import com.praktikum.trassify.R
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.unit.sp

object TextType{
    val text20Bold = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_bold)),
        fontSize = 20.sp
    )

    val text25SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 25.sp
    )

    val text20SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 20.sp
    )

    val text16SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 16.sp
    )

    val text15SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 15.sp
    )

    val text14SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 14.sp
    )


    val text10SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 10.sp
    )

    val text8SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 8.sp
    )

    val text5SemiBold = TextStyle(
        fontFamily = FontFamily(Font((R.font.mont_semi_bold))),
        fontSize = 5.sp
    )

    val text20Md = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_md)),
        fontSize = 20.sp
    )

    val text17Md = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_md)),
        fontSize = 17.sp
    )

    val text16Md = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_md)),
        fontSize = 16.sp
    )

    val text12Md = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_md)),
        fontSize = 12.sp
    )

    val text9Md = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_md)),
        fontSize = 9.sp
    )

    val text15Rg = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_rg)),
        fontSize = 15.sp
    )

    val text13Rg = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_rg)),
        fontSize = 13.sp
    )

    val text12Rg = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_rg)),
        fontSize = 12.sp
    )

    val text9Rg = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_rg)),
        fontSize = 9.sp
    )

    val text8Rg = TextStyle(
        fontFamily = FontFamily(Font(R.font.mont_rg)),
        fontSize = 8.sp
    )
}